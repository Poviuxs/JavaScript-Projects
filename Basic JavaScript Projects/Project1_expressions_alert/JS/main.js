
// Assigning the 1st string to the Sent1 variable
var Sent1 = "This is the beginning of the string";

// Assigning the 2nd string to the Sent2 variable
var Sent2 = " and this is the end of the string.";

// Concatenating Sent1 and Sent2 into one full sentence
var fullSentence = Sent1 + Sent2;

/*
Using window.alert() to display 
*/
window.alert(fullSentence);

/*
 concatenated sentence directly
in the HTML body.
*/
document.write(30*20);
